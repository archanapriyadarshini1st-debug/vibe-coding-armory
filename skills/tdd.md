# Test-Driven Development

Use when correctness and regression safety matter.

## Focus
- write a focused failing test for expected behavior
- implement the smallest change that passes it
- refactor without changing behavior
- keep tests fast and deterministic
- cover critical user flows and edge cases

## Pair with
`systematic-debugging` for failures and `webapp-testing` for browser-level behavior.
