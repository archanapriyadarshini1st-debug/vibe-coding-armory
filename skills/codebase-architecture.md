# Codebase Architecture

Use when a project is becoming difficult to extend or reason about.

## Focus
- identify responsibility boundaries
- reduce circular dependencies and duplication
- establish clear module ownership
- separate domain logic from UI/infrastructure where useful
- refactor incrementally with tests protecting behavior
- optimize for maintainability, not abstraction for its own sake
