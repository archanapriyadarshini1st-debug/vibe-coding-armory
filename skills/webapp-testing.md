# Web App Testing Skill

Use after building an app to verify real user flows instead of trusting the generated code.

## Focus
- smoke tests for critical flows
- browser-level interaction checks
- responsive verification
- console/network error detection
- regression checks after UI changes
- capture evidence for failures

## Pair with
`agent-browser` and `web-design-guidelines`.
